version: '3.8'

services:
  app:
    image: ghcr.io/orakysec/ferreira3dstudio:latest
    networks:
      - YkaroNET
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      labels:
        - "traefik.enable=true"
        - "traefik.docker.network=YkaroNET"
        - "traefik.http.routers.f3dsite.rule=Host(`ferreira3d.com`) || Host(`www.ferreira3d.com`)"
        - "traefik.http.routers.f3dsite.entrypoints=websecure"
        - "traefik.http.routers.f3dsite.tls.certresolver=letsencryptresolver"
        - "traefik.http.services.f3dsite.loadbalancer.server.port=80"

networks:
  YkaroNET:
    external: true
